# New packages needed on top of what you already have.
# Note: no anthropic/voyageai keys needed anymore -- everything below is local.
ollama          # python client for the local Ollama server
gradio
tabulate        # needed for pandas DataFrame.to_markdown() used in rag_pipeline.py

# One-time setup (not pip installs):
#   1. Install Ollama: https://ollama.com/download
#   2. ollama pull qwen2.5:14b        # routing + generation
#   3. ollama pull nomic-embed-text   # article embeddings
#   4. ollama serve                  # if not already running as a service
#   5. python retrieval.py --build   # re-embed with nomic-embed-text (old Voyage
#                                     # vectors are a different dimensionality
#                                     # and won't work with the new query embeddings)
